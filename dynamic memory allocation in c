#include <stdio.h>
#include <malloc.h>


int main()
{
    int *a;
    int i;
    a=(int*)malloc(sizeof(int)*5);
    for(i=0;i<=4;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<=4;i++)
    {
        printf("%d",&a[i]);
    }
    
    }
    
